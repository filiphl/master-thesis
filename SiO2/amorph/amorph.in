include "system.in"


variable N equal 5000
variable T equal 293

velocity	all create 5000 277387 mom yes

neighbor	0.3 bin
neigh_modify	delay 10

thermo		100
timestep	0.002

restart ${N} restartFiles/SiO2_*.restart
#dump myDump all atom ${N} dumpFiles/SiO2_*.dump


fix nvt all nvt temp 5000 5000 1.0
run ${N}

unfix nvt
fix nvt all nvt temp ${T} ${T} 1.0
run ${N}
