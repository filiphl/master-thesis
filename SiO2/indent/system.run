include "system.in"
velocity	all create 1 277387 mom yes


include "system.prepare"

variable N equal 5000
variable T equal 293


neighbor	0.3 bin
neigh_modify	delay 10

thermo		1000
timestep	0.002

fix nvt all nvt temp ${T} ${T} 1.0

restart 10000 restartFiles/SiO2_*.restart
dump myDump all atom 100 dumpFiles/SiO2_*.dump

run ${N}
unfix upperFreezeID
run ${N}
