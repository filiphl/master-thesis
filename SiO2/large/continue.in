include "system.in.init"

read_restart ${filename}

variable N equal 1
variable T equal 293
variable compressionLength equal 10

include "system.in.settings"

#Freeze the "bottom" of the slab
fix freezeID bottomAtomsID setforce 0 0 0
velocity bottomAtomsID set 0 0 0

restart 1000 restartFiles/initial*.restart
neighbor	0.3 bin
neigh_modify	delay 10

thermo		100
timestep	0.002

dump myDump all atom 500 dumpFiles/initial*.dump
dump surfaseDump surfaceGroup atom 500 dumpFiles/surface*.dump
fix deform all deform 1 z delta 0 -${compressionLength} remap x

fix nvt all nvt temp ${T} ${T} 1.0
run ${N}
